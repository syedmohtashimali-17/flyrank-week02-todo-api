# Stage 7 — My prompt (written from memory, no copying)

> Build a CRUD API in Node.js using Express for managing a to-do list. Tasks
> have an id (number), a title (string), and a done (boolean) field. Store
> tasks in an in-memory array, seeded with 3 example tasks. Implement these
> endpoints: GET /tasks (list all), GET /tasks/:id (get one, 404 if it
> doesn't exist), POST /tasks (create a task from a title in the request
> body, 201 on success, 400 if title is missing or empty), PUT /tasks/:id
> (update a task, 404 if not found, 400 for bad input), and DELETE /tasks/:id
> (remove a task, 204 on success, 404 if not found). Add Swagger UI at /docs
> so I can test the API interactively. Keep it as one file if possible.

Sent to Claude (a fresh conversation, no context from my hand-built version).
